#include <iostream>
using namespace std;

int main() {
    char a;
    cin >> a;

    if (a >= 'a' && a <= 'z') {
        cout << "Lowercase";
    }
    else if (a >= 'A' && a <= 'Z') {
        cout << "Uppercase";
    }
    else {
        cout << "Not an alphabet";
    }

    return 0;
}
